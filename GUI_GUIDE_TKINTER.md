# Discord 音樂機器人 - Tkinter GUI 使用指南

## 📋 概述

Tkinter 版本的 GUI 監控界面提供了一個美觀的桌面應用程序，用於實時監控 Discord 音樂機器人的運行狀態、播放資訊和日誌。

## ✨ 功能特性

### 1. 機器人狀態監控（左上角）
- ✅/❌ 機器人在線狀態
- ✅/❌ Lavalink 伺服器連接狀態（localhost:2333）
- 📊 連接的伺服器數量
- 🔊 當前語音連接數

### 2. 當前播放資訊（左下角）
- 🎵 正在播放的歌曲名稱
- 👤 歌曲請求者
- 🔁/🔂 播放模式（歌單循環/單曲重複）
- 🔊 當前音量百分比
- ▶️/⏸️/⏹️ 播放狀態

### 3. 播放佇列顯示（右側）
- 顯示前 10 首等待播放的歌曲
- 顯示歌曲名稱和請求者
- 超過 10 首顯示「還有 X 首歌曲」
- 支持滾動查看
- 實時更新佇列變化

### 4. 實時日誌查看（底部）
- **只顯示 GUI 啟動後的新日誌**（不顯示歷史記錄）
- 顏色區分日誌級別：
  - 🟢 綠色 = INFO（正常資訊）
  - 🟡 黃色 = WARNING（警告）
  - 🔴 紅色 = ERROR（錯誤）
  - 🔵 藍色 = SYSTEM（系統訊息）
- 自動滾動到最新日誌
- 自動限制最大行數（保留最新 1000 行）

## 🚀 快速開始

### 前置需求

1. Python 3.8+（Tkinter 已包含在標準庫中）
2. Discord 機器人正在運行
3. Lavalink 伺服器正在運行

### 啟動方式

#### 方法 1：使用啟動腳本（推薦）

**Windows:**
```bash
start_dashboard_tkinter.bat
```

**Linux/macOS:**
```bash
python3 dashboard_tkinter.py
```

#### 方法 2：手動啟動

```bash
python dashboard_tkinter.py
```

### 啟動順序建議

為了獲得最佳體驗，建議按以下順序啟動：

1. **啟動 Lavalink 伺服器（終端 1）**
   ```bash
   cd lavalink
   java -jar Lavalink.jar
   ```
   ✅ 等待看到：`Lavalink is ready to accept connections`

2. **啟動 Discord 機器人（終端 2）**
   ```bash
   python main_onlymusic.py
   ```
   ✅ 等待看到：
   - `機器人已上線`
   - `✓ 已連接到 Lavalink 伺服器`
   - `✓ 狀態導出任務已啟動`

3. **啟動 GUI 控制台（終端 3 或雙擊腳本）**
   ```bash
   python dashboard_tkinter.py
   ```
   ✅ 應該會彈出桌面窗口

## 🎨 界面佈局

```
┌────────────────────────────────────────────────────────┐
│  Discord 音樂機器人監控 - Tkinter 版本                │
├─────────────────────────┬──────────────────────────────┤
│  🤖 機器人狀態          │    📜 播放佇列              │
│  狀態: ✅ 在線          │    1. 歌曲A - 用戶A         │
│  Lavalink: ✅ 已連接    │    2. 歌曲B - 用戶B         │
│  伺服器數: 3            │    3. 歌曲C - 用戶C         │
│  語音連接: 1            │    ...                      │
├─────────────────────────┤                              │
│  🎵 當前播放            │                              │
│  歌曲: 歌曲名稱         │                              │
│  請求者: 用戶名         │                              │
│  模式: 🔁 歌單循環      │                              │
│  音量: 100%             │                              │
│  狀態: ▶️ 播放中        │                              │
├─────────────────────────┴──────────────────────────────┤
│  📋 實時日誌                                           │
│  [10:30:15] INFO - 正在播放: 歌曲名稱                 │
│  [10:29:58] INFO - 用戶 UserA 加入頻道                │
│  [10:29:45] INFO - 添加歌曲到佇列                     │
│  [自動滾動...]                                         │
└────────────────────────────────────────────────────────┘
```

### 布局比例
- **左側面板**：33% 寬度（機器人狀態 + 當前播放）
- **右側佇列**：67% 寬度
- **頂部區域**：50% 高度（狀態 + 播放資訊 + 佇列）
- **底部日誌**：50% 高度

## 🔧 工作原理

### 數據同步機制

1. **機器人端（main_onlymusic.py）**
   - 每 2 秒自動將狀態導出到 `bot_status.json`
   - 包含機器人狀態、播放資訊、佇列資訊

2. **GUI 端（dashboard_tkinter.py）**
   - **JSON 更新線程**：每 2 秒讀取 `bot_status.json` 更新界面
   - **日誌追蹤線程**：實時追蹤 `logs/music_bot.log`（**只讀新行**）

3. **線程安全通信**
   - 使用 `queue.Queue` 在背景線程和主線程間傳遞數據
   - 主線程定期處理隊列並更新 UI（每 100ms）

### 狀態文件（bot_status.json）

```json
{
  "bot_online": true,
  "lavalink_online": true,
  "guild_count": 3,
  "voice_connections": 1,
  "current_playing": {
    "title": "歌曲名稱",
    "requester": "用戶名",
    "mode": "🔁 歌單循環",
    "volume": 100,
    "status": "▶️ 播放中"
  },
  "queue": [
    "歌曲A - 用戶A",
    "歌曲B - 用戶B"
  ]
}
```

## 🆕 日誌顯示特性（重要）

### 與 Textual 版本的區別

| 特性 | Textual 版本 | Tkinter 版本 |
|------|-------------|--------------|
| 歷史日誌 | 顯示最後 50 行 | **不顯示歷史** |
| 新日誌 | 實時追加 | 實時追加 |
| 啟動訊息 | 無 | 顯示啟動時間 |

### 日誌追蹤實現

**關鍵技術：`f.seek(0, 2)`**

```python
with open(log_file, 'r', encoding='utf-8') as f:
    # 直接移動到文件末尾（跳過所有歷史日誌）
    f.seek(0, 2)  # SEEK_END

    # 只讀取新寫入的行
    while running:
        line = f.readline()
        if line:
            # 只有新日誌會被顯示
            display_log(line)
```

### 日誌級別檢測

GUI 會自動檢測日誌內容並應用顏色：

```
2026-01-06 10:30:15 - MusicBot - INFO - 訊息    → 綠色
2026-01-06 10:30:15 - MusicBot - WARNING - 警告 → 黃色
2026-01-06 10:30:15 - MusicBot - ERROR - 錯誤   → 紅色
[10:30:15] GUI 啟動...                         → 藍色（系統訊息）
```

## 🐛 故障排除

### 問題 1：GUI 啟動後顯示「bot_status.json 文件不存在」

**原因：** 機器人未啟動或狀態導出任務未運行

**解決方案：**
1. 確認機器人正在運行：`python main_onlymusic.py`
2. 檢查機器人日誌是否顯示「✓ 狀態導出任務已啟動」
3. 等待 2 秒讓機器人生成 `bot_status.json` 文件

### 問題 2：GUI 顯示但數據不更新

**原因：** `bot_status.json` 未更新

**解決方案：**
1. 檢查機器人是否正在運行
2. 查看 `bot_status.json` 文件的修改時間（應該每 2 秒更新）
3. 重啟機器人

### 問題 3：日誌區域為空或只顯示啟動訊息

**原因：** 這是正常的！Tkinter 版本**只顯示新日誌**

**解決方案：**
- 無需解決，這是設計功能
- 機器人產生新日誌時會自動顯示
- 例如：播放歌曲、跳過歌曲、添加歌曲到佇列等操作

### 問題 4：GUI 窗口凍結或無響應

**原因：** 線程死鎖或主線程阻塞

**解決方案：**
1. 關閉 GUI 窗口
2. 檢查是否有錯誤訊息
3. 重新啟動 GUI

### 問題 5：中文顯示為亂碼或方塊

**原因：** 系統字體不支援中文

**解決方案：**
- Windows：通常無此問題（使用系統默認字體）
- Linux：安裝中文字體（如 `sudo apt install fonts-noto-cjk`）
- macOS：使用系統默認字體即可

### 問題 6：日誌顏色不正確

**原因：** 日誌格式變更

**解決方案：**
- 檢查 `logs/music_bot.log` 的格式
- GUI 通過檢測 "INFO"、"WARNING"、"ERROR" 關鍵字來著色
- 確保日誌包含這些關鍵字

## 💡 使用技巧

### 1. 多窗口監控
- 可以同時開啟多個終端和 GUI
- 一個運行機器人，一個運行 Lavalink，一個運行 GUI
- 便於同時查看多個輸出

### 2. 最小化到系統托盤
- Windows：點擊最小化按鈕
- GUI 繼續在背景運行並更新數據

### 3. 調整窗口大小
- 可以拖動窗口邊緣調整大小
- 最小窗口大小：900x600
- 推薦窗口大小：1200x800

### 4. 查看完整日誌
- GUI 只顯示新日誌（最新 1000 行）
- 查看完整歷史日誌：
  ```bash
  cat logs/music_bot.log
  # 或 Windows
  type logs\music_bot.log
  ```

### 5. 性能優化
- GUI 資源占用極低（約 20-30 MB 記憶體）
- 可以長時間運行而不影響機器人性能
- 不需要時直接關閉窗口即可

## 🔄 更新日誌

### v1.0.0 (2026-01-12)
- ✨ 首次發布 Tkinter 版本
- ✅ 實時監控機器人狀態
- ✅ 顯示當前播放和佇列
- ✅ **只顯示啟動後的新日誌**（不顯示歷史）
- ✅ 桌面 GUI（不再是終端 UI）
- ✅ 線程安全的數據更新
- ✅ 顏色區分的日誌顯示

## 📞 支援

### 常見問題檢查清單

啟動 GUI 前：
- [ ] Lavalink 正在運行（`java -jar Lavalink.jar`）
- [ ] 機器人正在運行（`python main_onlymusic.py`）
- [ ] 機器人日誌顯示「✓ 狀態導出任務已啟動」
- [ ] 存在 `bot_status.json` 文件

GUI 運行中：
- [ ] 機器人狀態顯示「✅ 在線」
- [ ] Lavalink 狀態顯示「✅ 已連接」
- [ ] 日誌顯示啟動訊息
- [ ] 播放歌曲時日誌實時更新

### 查看系統訊息

- 查看機器人日誌：`logs/music_bot.log`
- 查看 Lavalink 日誌：`lavalink/logs/`
- 查看狀態文件：`bot_status.json`

---

## 🎉 對比：Textual vs Tkinter

### Textual 版本（終端 UI）
- ✅ 輕量級，任何終端都能運行
- ✅ 支持 SSH 遠程監控
- ✅ 顯示歷史日誌（最後 50 行）
- ❌ 需要安裝 textual 依賴

### Tkinter 版本（桌面 GUI）
- ✅ 真正的桌面應用
- ✅ 無需額外依賴（Tkinter 是標準庫）
- ✅ **只顯示新日誌**（符合新需求）
- ✅ 更美觀的界面和顏色
- ❌ 無法通過 SSH 使用（除非 X11 轉發）

---

**享受美觀的桌面 GUI 監控體驗！** 🎉
