# Discord 音樂機器人 - GUI 控制台使用指南

## 📋 概述

本項目提供了一個美觀的終端 GUI 控制台，用於實時監控 Discord 音樂機器人的運行狀態、播放資訊和日誌。

## ✨ 功能特性

### 1. 機器人狀態監控
- ✅/❌ 機器人在線狀態
- ✅/❌ Lavalink 伺服器連接狀態
- 📊 連接的伺服器數量
- 🔊 當前語音連接數

### 2. 當前播放資訊
- 🎵 正在播放的歌曲名稱
- 👤 歌曲請求者
- 🔁/🔂 播放模式（歌單循環/單曲重複）
- 🔊 當前音量
- ▶️/⏸️/⏹️ 播放狀態

### 3. 播放佇列顯示
- 顯示前 10 首等待播放的歌曲
- 顯示歌曲名稱和請求者
- 實時更新佇列變化

### 4. 實時日誌查看
- 自動追蹤最新日誌
- 顏色區分日誌級別：
  - 🟢 綠色 = INFO（正常資訊）
  - 🟡 黃色 = WARNING（警告）
  - 🔴 紅色 = ERROR（錯誤）
- 自動滾動到最新日誌
- 保留最近 50 條歷史日誌

## 🚀 快速開始

### 安裝依賴

```bash
# 安裝所有必要的依賴（包括 Textual）
pip install -r requirements.txt
```

### 啟動 GUI

#### 方法 1：使用啟動腳本（推薦）

**Windows:**
```bash
start_dashboard.bat
```

**Linux/macOS:**
```bash
python dashboard.py
```

#### 方法 2：手動啟動

```bash
python dashboard.py
```

### 啟動順序建議

為了獲得最佳體驗，建議按以下順序啟動：

1. **啟動 Lavalink 伺服器**
   ```bash
   cd lavalink
   java -jar Lavalink.jar
   ```

2. **啟動 Discord 機器人**
   ```bash
   python main_onlymusic.py
   ```

3. **啟動 GUI 控制台**
   ```bash
   python dashboard.py
   ```

## ⌨️ 快捷鍵

| 快捷鍵 | 功能 | 說明 |
|--------|------|------|
| `Q` | 退出 | 關閉 GUI 控制台 |
| `R` | 手動刷新 | 立即刷新所有數據 |
| `L` | 清除日誌 | 清除日誌顯示區域 |
| `S` | 保存日誌 | 將當前日誌保存到文件 |

## 🎨 界面佈局

```
┌─────────────────────── Discord 音樂機器人控制台 ───────────────────────┐
│                                                                        │
│  ┌──────────── 機器人狀態 ──────────┐  ┌────── 播放佇列 ──────┐      │
│  │ 狀態: ✅ 在線                     │  │ 1. 歌曲A - 用戶A     │      │
│  │ Lavalink: ✅ 已連接               │  │ 2. 歌曲B - 用戶B     │      │
│  │ 伺服器數: 3  |  語音連接: 1       │  │ 3. 歌曲C - 用戶C     │      │
│  └──────────────────────────────────┘  └─────────────────────┘      │
│                                                                        │
│  ┌──────────── 當前播放 ────────────┐                                │
│  │ 🎵 歌曲: 歌曲名稱                 │                                │
│  │ 👤 請求者: 用戶名                 │                                │
│  │ 🔁 模式: 歌單循環                 │                                │
│  │ 🔊 音量: 100%                     │                                │
│  │ ▶️ 狀態: 播放中                   │                                │
│  └──────────────────────────────────┘                                │
│                                                                        │
│  ┌────────────────── 實時日誌 ──────────────────┐                    │
│  │ [14:30:15] INFO - 正在播放: 歌曲名稱          │                    │
│  │ [14:29:58] INFO - 用戶 UserA 加入頻道         │                    │
│  │ [14:29:45] INFO - 添加歌曲到佇列              │                    │
│  │ [自動滾動...]                                  │                    │
│  └───────────────────────────────────────────────┘                    │
│                                                                        │
│  Q 退出 | R 刷新 | L 清除日誌 | S 保存日誌                            │
└────────────────────────────────────────────────────────────────────┘
```

## 🔧 工作原理

### 數據同步機制

1. **機器人端（main_onlymusic.py）**
   - 每 2 秒自動將狀態導出到 `bot_status.json`
   - 包含機器人狀態、播放資訊、佇列資訊

2. **GUI 端（dashboard.py）**
   - 每 2 秒讀取 `bot_status.json` 更新界面
   - 實時追蹤 `logs/music_bot.log` 顯示日誌

3. **狀態文件（bot_status.json）**
   ```json
   {
     "bot_online": true,
     "lavalink_online": true,
     "guild_count": 3,
     "voice_connections": 1,
     "current_playing": {
       "title": "歌曲名稱",
       "requester": "用戶名",
       "mode": "🔁 歌單循環",
       "volume": 100,
       "status": "▶️ 播放中"
     },
     "queue": [
       "歌曲A - 用戶A",
       "歌曲B - 用戶B"
     ]
   }
   ```

## 🐛 故障排除

### 問題 1：GUI 顯示「日誌文件不存在」

**原因：** 機器人尚未創建日誌文件

**解決方案：**
1. 確保機器人已經啟動
2. 等待 5 秒後 GUI 會自動重試
3. 檢查 `logs/` 目錄是否存在

### 問題 2：狀態資訊不更新

**原因：** `bot_status.json` 文件未生成或機器人未運行

**解決方案：**
1. 確認機器人正在運行
2. 檢查是否存在 `bot_status.json` 文件
3. 查看機器人日誌是否有「✓ 狀態導出任務已啟動」訊息
4. 按 `R` 鍵手動刷新

### 問題 3：GUI 啟動失敗

**原因：** 缺少 Textual 依賴

**解決方案：**
```bash
pip install textual>=0.47.0
# 或
pip install -r requirements.txt
```

### 問題 4：日誌顯示亂碼

**原因：** 終端編碼問題

**解決方案（Windows）：**
```bash
# 在 PowerShell 中執行
$OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001
python dashboard.py
```

### 問題 5：界面顯示不正常

**原因：** 終端窗口太小

**解決方案：**
- 調整終端窗口大小至少 120x30 字符
- 使用全屏模式獲得最佳體驗

## 📝 保存日誌

### 手動保存

按 `S` 鍵可將當前顯示的日誌保存到文件：
- 保存位置：`logs/dashboard_log_YYYYMMDD_HHMMSS.txt`
- 文件名包含時間戳，便於歸檔

### 自動日誌輪轉

機器人日誌（`logs/music_bot.log`）會自動輪轉：
- 每個日誌文件最大 5MB
- 保留最近 5 個備份文件
- 總計最多 25MB 日誌存儲

## 🎯 使用技巧

1. **多窗口監控**
   - 可以同時開啟多個終端
   - 一個運行機器人，一個運行 GUI
   - 便於同時查看兩者的輸出

2. **遠程監控**
   - 使用 SSH 連接到伺服器
   - 運行 `python dashboard.py` 即可遠程監控
   - 建議使用 `tmux` 或 `screen` 保持會話

3. **日誌分析**
   - 使用 `S` 鍵保存日誌後
   - 可以用文本編輯器搜索特定錯誤
   - 便於調試和問題追蹤

4. **性能優化**
   - GUI 每 2 秒更新一次，資源占用極低
   - 可以長時間運行而不影響機器人性能
   - 不需要時直接按 `Q` 退出即可

## 🔄 更新日誌

### v1.0.0 (2026-01-12)
- ✨ 首次發布
- ✅ 實時監控機器人狀態
- ✅ 顯示當前播放和佇列
- ✅ 實時日誌追蹤
- ✅ 快捷鍵控制

## 📞 支援

如果遇到問題：
1. 查看本指南的「故障排除」章節
2. 檢查機器人日誌：`logs/music_bot.log`
3. 檢查狀態文件：`bot_status.json`
4. 確保所有依賴已正確安裝

---

**享受美觀的 GUI 監控體驗！** 🎉
